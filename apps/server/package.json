{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.2",
    "@trpc/server": "catalog:",
    "@yapper/api": "workspace:*",
    "@yapper/auth": "workspace:*",
    "@yapper/db": "workspace:*",
    "@yapper/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "@yapper/config": "workspace:*",
    "alchemy": "catalog:",
    "tsdown": "^0.22.3",
    "typescript": "catalog:",
    "wrangler": "^4.107.0"
  }
}
